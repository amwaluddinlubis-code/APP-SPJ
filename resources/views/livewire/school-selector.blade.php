<div class="mt-6 space-y-3"><x-ui.input wire:model.live.debounce.300ms="search" placeholder="Cari nama sekolah atau NPSN..." />@forelse($schools as $school)<article wire:key="school-{{ $school->id }}" class="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-[var(--ui-line)] bg-[var(--ui-surface-soft)] p-4"><div><p class="font-bold text-slate-800">{{ $school->name }}</p><p class="mt-1 text-xs text-slate-500">NPSN {{ $school->npsn }} · Database {{ $school->databaseRecord?->status ?? 'Belum diprovisikan' }}</p></div><x-ui.button type="button" wire:click="selectSchool({{ $school->id }})">Pilih Sekolah</x-ui.button></article>@empty<div class="rounded-lg bg-[var(--ui-surface-soft)] p-5 text-base text-slate-500">Belum ada sekolah yang cocok.</div>@endforelse</div>
