<div class="mt-6 space-y-3"><x-ui.input wire:model.live.debounce.300ms="search" placeholder="Cari tahun anggaran..." />@forelse($years as $year)<article wire:key="year-{{ $year->id }}" class="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-[var(--ui-line)] bg-[var(--ui-surface-soft)] p-4"><div><p class="font-bold text-slate-800">Tahun {{ $year->year }}</p><p class="mt-1 text-xs text-slate-500">{{ $year->fundSource?->code }} · {{ $year->fundSource?->name ?? $year->fund_source }}</p></div><x-ui.button type="button" wire:click="selectYear({{ $year->id }})">Gunakan</x-ui.button></article>@empty<div class="rounded-lg bg-[var(--ui-surface-soft)] p-5 text-base text-slate-500">Belum ada kombinasi tahun dan sumber dana.</div>@endforelse</div>
